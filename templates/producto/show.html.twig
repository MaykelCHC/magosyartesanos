{% extends 'base.html.twig' %}

{% block title %}Producto{% endblock %}

<<<<<<< Updated upstream
{% block body %}
    <h1>Producto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ producto.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ producto.nombre }}</td>
            </tr>
            <tr>
                <th>Imagenproducto</th>
                <td>{{ producto.imagenproducto }}</td>
            </tr>
            <tr>
                <th>Beneficioxventa</th>
                <td>{{ producto.beneficioxventa }}</td>
            </tr>
            <tr>
                <th>Tipomoneda</th>
                <td>{{ producto.tipomoneda }}</td>
            </tr>
            <tr>
                <th>Iva</th>
                <td>{{ producto.iva }}</td>
            </tr>
            <tr>
                <th>IDD</th>
                <td>{{ producto.IDD }}</td>
            </tr>
            <tr>
                <th>Stock</th>
                <td>{{ producto.Stock }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_producto_index') }}">back to list</a>

    <a href="{{ path('app_producto_edit', {'id': producto.id}) }}">edit</a>

    {{ include('producto/_delete_form.html.twig') }}
=======
{% block page %}
    <!-- Content Wrapper. Contains page content -->
    <div class="content-wrapper">
        <!-- Content Header (Page header) -->
        <div class="content-header">
            <div class="container-fluid">
                <div class="card">
                    <div class="card-header">
                        <div class="card-title">
                            <h3>Producto</h3>
                        </div>
                    </div>
                    <table class="table">
                        <tbody>
                        <tr>
                            <th>ID</th>
                            <td>{{ producto.id }}</td>
                        </tr>
                        <tr>
                            <th>Nombre</th>
                            <td>{{ producto.nombre }}</td>
                        </tr>
                        <tr>
                            <th>Imagen del Producto</th>
                            <td><img width="50" height="50" src="{{ asset('uploads/photos/' ~ producto.imagen) }}"
                                     alt="Foto Producto"></td>
                        </tr>
                        <tr>
                            <th>Beneficio por Venta</th>
                            <td>{{ producto.beneficioxventa }}</td>
                        </tr>
                        <tr>
                            <th>Tipo de Moneda</th>
                            <td>{{ producto.tipodemoneda }}</td>
                        </tr>
                        <tr>
                            <th>I.V.A.</th>
                            <td>{{ producto.iv }}</td>
                        </tr>
                        <tr>
                            <th>IDD</th>
                            <td>{{ producto.IDD }}</td>
                        </tr>
                        <tr>
                            <th>Stock</th>
                            <td>{{ producto.Stock }}</td>
                        </tr>
                        <tr>

                            <th>Usuario Asignado</th>

                            <td class="form-group">
                                <label>
                                    <select multiple="" class="custom-select" disabled="">
                                        {% for usuarioid in producto.usuarioid %}
                                            <option>{{ usuarioid }}</option>
                                        {% endfor %}
                                    </select>
                                </label>
                            </td>

                        </tr>
                        </tbody>
                    </table>

                    <div style="padding: 20px">
                        <a class="btn btn-danger" style="float: left"
                           href="{{ path('app_producto_index') }}">Regresar</a>

                        <a class="btn btn-success" style="float: right"
                           href="{{ path('app_producto_edit', {'id': producto.id}) }}">Editar</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
>>>>>>> Stashed changes
{% endblock %}
