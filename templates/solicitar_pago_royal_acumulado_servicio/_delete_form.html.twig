<form method="post" action="{{ path('app_solicitar_pago_royal_acumulado_servicio_delete', {'id': solicitar_pago_royal_acumulado_servicio.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ solicitar_pago_royal_acumulado_servicio.id) }}">
    <button class="btn">Delete</button>
</form>
